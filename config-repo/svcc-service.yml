server:
  port: 8090
  
msg: zhangxd

eureka:
  instance:
    metadata-map:
      user.name: ${security.user.name}
      user.password: ${security.user.password}

security:
  user:
    name: user
    password: password
  oauth2:
    client:
      clientId: svcc-service
      clientSecret: ${security.user.password}
      accessTokenUri: http://${authserver.hostname}:${authserver.port}${authserver.contextPath}/oauth/token
      grant-type: client_credentials
      scope: server
      
#公共配置与profiles选择无关
 mybatis:
  typeAliasesPackage: cn.zhangxd.svcc.entity
  mapperLocations: classpath:cn/zhangxd/svcc/entity/*.xml


---

#开发配置
spring:
#  profiles: dev
  datasource:
      type: com.alibaba.druid.pool.DruidDataSource
      initialSize: 5
      minIdle: 5
      maxActive: 20
      # 配置获取连接等待超时的时间  
      maxWait: 60000
      # 配置间隔多久才进行一次检测，检测需要关闭的空闲连接，单位是毫秒 
      timeBetweenEvictionRunsMillis: 60000
      # 配置一个连接在池中最小生存的时间，单位是毫秒
      minEvictableIdleTimeMillis: 300000
      validationQuery: SELECT 1 #FROM DUAL
      testWhileIdle: true
      testOnBorrow: false
      testOnReturn: false
      # 打开PSCache，并且指定每个连接上PSCache的大小  
      poolPreparedStatements: true
      maxPoolPreparedStatementPerConnectionSize: 20
      # 配置监控统计拦截的filters，去掉后监控界面sql无法统计，'wall'用于防火墙  
      filters: stat,wall,log4j
      # 通过connectProperties属性来打开mergeSql功能；慢SQL记录 
      connectionProperties: druid.stat.mergeSql=true;druid.stat.slowSqlMillis=5000
      master:
        url: jdbc:sqlserver://192.169.1.22:1433;databaseName=cdzy_jwgl1db
        username: jwbrowse
        password: cdzy_jc_2014
        driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
      cluster:
        url: jdbc:sqlserver://218.75.139.57:1433;databaseName=website
        username: qgjc
        password: "20170414#jisb#*)@"
        driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
 
